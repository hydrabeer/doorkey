package password_validation.entity;

/**
 * The implementation for Entropy Calculation.
 */
public class EntropyCalculator {

    /**
     * The algorithm for calculaing entropy.
     *
     * @param password the password for which we are calculating entropy.
     * @return the final value of entropy/strength
     */
    public double calculateEntropy(String password) {
        return 0.0;

    }
}
