package interface_adapter.vault_imports;

/**
 * The possible choices for password manager inputs.
 */
public enum PasswordManager {
    ONE_PASSWORD,
    // Test value for unsupported password manager
    UNSUPPORTED
}
